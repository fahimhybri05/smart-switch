# Copy to .env and fill in real values before running.

DATABASE_URL=postgres://smartswitch:smartswitch@localhost:5432/smartswitch
JWT_SECRET=change-me-to-a-long-random-string
PORT=3000

# Set to the number of reverse-proxy hops in front of this server (e.g. 1
# for a single nginx). Leave unset when the server is reachable directly.
# TRUST_PROXY=1
